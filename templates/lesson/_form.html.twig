{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-warning m-3 p-3" style="width: 16rem;">{{ button_label|default('Сохранить') }}</button>
{{ form_end(form) }}
