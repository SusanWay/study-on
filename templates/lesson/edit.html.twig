{% extends 'base.html.twig' %}

{% block title %}Edit Lesson{% endblock %}

{% block body %}
<div class="container">
    <h1>Редактировать урок</h1>
    {{ include('lesson/_form.html.twig', {'button_label': 'Сохранить'}) }}
    
    <div class="d-flex">
    <a href="{{ path('app_lesson_index') }}" class="btn btn-warning m-3 p-3" style="width: 16rem;">Вернуться назад</a>
    {{ include('lesson/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
